spring.application.name=microservicio_laboratorios

#Logging
logging.level.root=INFO
logging.level.org.hibernate=INFO
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

#JSON formatting
spring.jackson.serialization.indent-output=true

#Oracle Database Configuration (usando variables de entorno)
spring.datasource.url=${DB_URL:jdbc:oracle:thin:@cryhzie2rbki7pdg_tp?TNS_ADMIN=/Wallet_CRYHZIE2RBKI7PDG}
spring.datasource.username=${DB_USERNAME:USER_DFSIII}
spring.datasource.password=${DB_PASSWORD:DUOC_ActSum_DFS3}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

#JPA/Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=false

#Connection Pool
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:10}
spring.datasource.hikari.connection-timeout=${DB_TIMEOUT:30000}
spring.datasource.hikari.minimum-idle=5

#Server
server.port=${SERVER_PORT:9090}

#Actuator para health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
